# Core orchestration
langgraph>=0.2.0
langchain-core>=0.2.0

# LLM
anthropic>=0.30.0

# HTTP client (streaming downloads)
httpx>=0.27.0

# Spreadsheet (minimal text dump only)
openpyxl>=3.1.0
xlrd>=2.0.1

# PowerPoint (minimal text dump only)
python-pptx>=1.0.0

# Image format conversion (tiff/bmp → png for Claude vision)
Pillow>=10.4.0

# Data validation
pydantic>=2.7.0

# Database
pymysql>=1.1.0

# Environment
python-dotenv>=1.0.0

# HTTP server (CRM integration)
fastapi>=0.110.0
uvicorn>=0.29.0

# Word documents
python-docx>=1.1.0
